# 테스트

```powershell
.\.venv\Scripts\python.exe -m ruff check src tests
.\.venv\Scripts\python.exe -m pytest -q
```

테스트는 로컬 `.env`와 무관하게 백그라운드 폴러·발송 워커를 끄고 임시 SQLite를 사용합니다. HubSpot·Slack·SMTP·WhatsApp 네트워크 호출은 모킹합니다. 라이브 계정으로 보내는 검증은 `SEND_OVERRIDE_EMAIL`에 내부 테스트 주소를 넣고 한 건씩 수행합니다.
