# Derived from pyproject.toml — install with: pip install -r requirements.txt
# For dev extras, use: pip install -e ".[dev]"
# For Postgres (Render/Supabase deploy), use: pip install -r requirements.txt -r requirements-postgres.txt
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.4
httpx>=0.27
python-dotenv>=1.0
jinja2>=3.1
anthropic>=0.39
