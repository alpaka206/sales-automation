Identify the primary natural language of the text below.

Rules:
- Output ONLY the ISO 639-1 two-letter code in lowercase (e.g. en, ko, ja, zh, vi, th, es, fr, de, pt, id). Nothing else — no punctuation, no explanation.
- Judge by the language the text is actually written in, NOT by any country or product names it mentions.
- If the text mixes languages, return the code of the language most of the sentences are written in.

Text:
"""
{{text}}
"""
