# 테스트

```powershell
.\.venv\Scripts\python.exe -m ruff check src tests
.\.venv\Scripts\python.exe -m pytest -q
```

테스트는 로컬 `.env`와 무관하게 백그라운드 폴러·발송 워커를 끄고 임시 SQLite를 사용합니다. HubSpot·Slack 네트워크 호출은 모킹합니다. 라이브 계정 검증은 별도 테스트 티켓의 기존 Conversations 스레드와 승인된 초안 한 건으로 수행하며, 애플리케이션이 수신자를 다른 주소로 바꾸지는 않습니다.
