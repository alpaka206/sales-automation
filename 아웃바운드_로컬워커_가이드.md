# 아웃바운드 로컬 워커 — 사용 가이드 (비개발자용)

## 0. 한 줄 요약

배포된 웹(Render)에서 **"이런 사람들 찾아줘"** 라고 입력하면, 그 요청이 공용 DB(Supabase)에
**대기열**로 쌓입니다. 내 PC에서 켜둔 **로컬 워커**가 그 대기열을 30초마다 확인해서
**실제 발굴(크롤링 → 점수 → 메일 초안)** 을 돌리고 결과를 다시 같은 DB에 저장합니다.
초안은 웹에서 사람이 **승인**해야만 발송됩니다.

> 즉, "배포된 곳에서 치면 내 PC에 설치된 게 작동된다" = **웹은 주문만 받고, 실제 작업은 내 PC가 한다.**

---

## 1. 왜 이렇게 나눴나?

발굴 작업은 웹 크롤링 + 브라우저 + AI 호출로 **무겁습니다**. Render 무료 서버는 메모리가
작고 브라우저(Chromium)도 없어서 발굴을 못 돌립니다. 그래서:

| 역할 | 어디서 도나 | 하는 일 |
|------|------------|---------|
| **웹(배포본)** | Render | 자연어 요청을 받아 소스 분류 → 대기열에 등록 / 결과 승인 |
| **로컬 워커** | 내 PC | 대기열을 가져와 실제 발굴 실행 → DB에 프로스펙트·초안 저장 |
| **공용 DB** | Supabase | 둘이 같은 DB를 봄 (요청·결과가 여기서 오감) |

핵심: **웹과 로컬 워커가 같은 `DATABASE_URL`(Supabase)을 봐야** 연결됩니다. (이미 그렇게 설정돼 있습니다.)

---

## 2. 처음 한 번만 (설치)

내 PC에 프로젝트 폴더가 있어야 합니다. (아래 5번 "남에게 넘길 때" 참고)

1. **`scripts\setup.bat` 더블클릭** — 파이썬 가상환경 생성 + 라이브러리 설치 + DB 점검을 자동으로 합니다.
   (1~2분. Python 3.11+ 가 먼저 설치돼 있어야 함. 없으면 안내문이 뜹니다.)
2. **`.env` 파일 확인** — 아래 값이 채워져 있으면 됩니다. (지금 이 PC는 이미 채워져 있습니다.)
   - `DATABASE_URL` → Supabase 주소 (웹과 **반드시 동일**)
   - `GOOGLE_CREDENTIALS_JSON` → Vertex AI 서비스계정 (AI용)
   - 발굴 소스 키: `YOUTUBE_API_KEY`, `GOOGLE_CSE_API_KEY`, `GOOGLE_CSE_ID`
3. (선택) **링크드인 댓글 소스**를 쓸 거면 브라우저가 필요합니다. 명령창에서 한 번:
   ```
   .venv\Scripts\activate
   playwright install chromium
   ```
   (유튜브 / 구글검색 / 채용공고 / CSV 소스는 이 단계가 **필요 없습니다.**)

---

## 3. 매번 쓸 때 (실행)

1. **`scripts\run_outbound_worker.bat` 더블클릭.**
   - 검은 창이 뜨고 "발굴 워커를 시작합니다" 가 나오면 켜진 겁니다.
   - **이 창을 켜둔 채로 두세요.** 30초마다 새 요청을 자동으로 가져와 실행합니다.
   - 대기 중에는 조용합니다(정상). 요청이 들어오면 로그가 주르륵 올라갑니다.
2. **웹(배포 URL)** 에서 `아웃바운드 → 발굴 시작` 에 자연어로 입력하고 "발굴 시작".
   - 예: `유튜브에서 다국어 자막 운영하는 채널 운영자 찾아줘`
   - "발굴 대기열에 등록됨" 이 뜨면 주문 완료.
3. 잠시 후(내 PC 워커가 처리) **웹 `프로스펙트`** 메뉴에 발굴된 사람들이 점수와 함께 나타납니다.
4. **`메시지`** 또는 프로스펙트에서 초안을 검토 → **"검토 완료·발송"** 눌러 승인. (승인 전엔 절대 안 나갑니다.)
5. 끝낼 때는 워커 창에서 **Ctrl+C** 또는 창 닫기.

> 워커가 꺼져 있으면 웹에서 주문만 쌓이고 실행이 안 됩니다. 발굴할 땐 워커 창을 꼭 켜두세요.

---

## 4. 잘 되는지 빠르게 테스트

워커 창을 켜둔 상태에서, 한 건만 즉시 돌려보고 싶으면 명령창에서:

```
.venv\Scripts\activate
python scripts\run_outbound_worker.py --query "유튜브에서 다국어 자막 운영하는 채널 운영자 찾아줘"
```

→ 실제로 유튜브에서 채널을 찾아 점수를 매기고 초안을 만들어 DB에 넣습니다. (검증 완료된 동작입니다.)

---

## 5. 남에게 넘길 때 (파일 최소화)

로컬 워커는 프로젝트 전체 코드를 사용하므로 "파일 1~2개"로는 안 됩니다. **가장 간단한 방법:**

1. **프로젝트 폴더를 통째로 ZIP** 으로 압축해서 전달. (단, `.venv`, `data`, `logs`, `__pycache__`,
   `node_modules` 폴더는 빼고 압축하면 용량이 확 줄고 받는 사람이 setup.bat으로 새로 만듭니다.)
2. **`.env` 파일** 1개를 따로 전달(또는 ZIP에 포함). ← 여기에만 비밀키가 들어있어 핵심입니다.

받는 사람은 → 압축 풀고 → `.env` 넣고 → `setup.bat` 더블클릭 → `run_outbound_worker.bat` 더블클릭. 끝.

> 정리하면 받는 사람이 신경 쓸 파일은 사실상 **`.env` 하나**이고, 나머지는 ZIP 하나 + 더블클릭 2번입니다.

---

## 6. 자주 막히는 곳

| 증상 | 원인 / 해결 |
|------|------------|
| 워커 창에 "`.venv가 없습니다`" | `setup.bat` 을 먼저 실행 |
| 웹에서 주문했는데 프로스펙트가 안 생김 | 워커 창이 꺼져 있음 → 켜두기. 또는 워커 PC의 `DATABASE_URL`이 웹과 다름 |
| "발굴 0건" | 소스 API 키 미설정(`YOUTUBE_API_KEY` 등) 또는 검색어가 너무 좁음 |
| 링크드인 댓글만 실패 | `playwright install chromium` 안 했음 (2-3 단계) |
| 신뢰도 부족 / 추가 정보 필요 메시지 | 요청을 더 구체적으로 (지역·직무·규모 등) |
```
